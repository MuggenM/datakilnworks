duckdb>=1.5.4,<1.6.0
deltalake==1.5.0
duckrun>=0.4.71   # also the dbt adapter (type: duckrun in dbt_project/profiles.yml); it builds on dbt-duckdb
dbt-core>=1.10.0  # dbt transformations (Transformations view, workflow dbt tasks); listed explicitly, duckrun also pulls it in
jinja2>=3.1.0     # dbt's SQL templating and the studio's HTML templates (Jinja2Templates)
sqlframe[duckdb]>=3.25.0
# In-Studio notebook kernels (there is no JupyterLab server)
ipykernel>=6.29.0
jupyter_client>=8.6.0
nbformat>=5.10.0
itables
pyarrow
pandas
openpyxl
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
croniter>=6.0.0
papermill>=2.6.0
APScheduler>=3.10.0
requests>=2.31.0
ldap3>=2.9.1
httpx>=0.27.0   # studio -> compute worker calls (was only present transitively through JupyterLab)
ray[default]>=2.35.0
azure-identity>=1.17.0
azure-storage-file-datalake>=12.14.0
cryptography>=41.0.0
boto3>=1.34.0    # Auto-Loader S3 sources (ListObjectsV2 polling, quarantine copies)
paramiko>=3.4.0      # Connections: SFTP sources for Auto-Loader (host key pinned per connection)
